syntax = "proto3";

import "database.proto";
import "state.proto";

message bzn_msg
{
    oneof msg {
        database_msg db = 10;
        state_request state = 11;
        string json = 12;
    }
}
